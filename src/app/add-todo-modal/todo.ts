export interface Todo {
    id: number;
    title: string;
    description: string;
    due_date: string; // Assuming due_date is a string, you can adjust the type accordingly
    priority: string; // Assuming priority is a string, you can adjust the type accordingly
    // You can add more properties as needed
  }
  